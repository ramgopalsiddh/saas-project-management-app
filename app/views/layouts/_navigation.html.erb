<nav class="navbar navbar-expand-lg fixed-top">
  <div class="container">
    <%= link_to 'Project Management', root_path, class: 'navbar-brand'%>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item" >
            <%= link_to 'All Accounts', accounts_path, class: "nav-link#{' active' if current_page?(accounts_path)}" %>
          </li>
          <li class="nav-item" >
            <%= link_to 'Projects', projects_path, class: "nav-link#{' active' if current_page?(projects_path)}" %>
          </li>
          <li class="nav-item" >
            <%= link_to  edit_user_registration_path, class: "nav-link#{' active' if current_page?(edit_user_registration_path)}" do%>
              Edit Profile 
            <% end %>
          </li>
        </ul>

        <ul class="navbar-nav mi-auto">
          <%if current_user %> 
            <li class="nav-item user-name me-2 mt-2">
              <strong><%= current_user.email %></strong>
            </li>
            <li class="nav-item me-2">
              <%= button_to("Log Out", destroy_user_session_path, method: :delete, class: 'btn btn-danger') %>
          <% else %>
              <%= button_to 'Register', new_user_registration_path, method: :get, class: 'btn btn-info' %>
              <%= button_to 'Login', new_user_session_path, method: :get, class: 'btn btn-primary ms-3' %>
            </li>
          <% end %>
        </ul>
      </div>
  </div>
</nav>